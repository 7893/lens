{
  "name": "@lens/engine",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'No build step'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup:local-db": "yes | wrangler d1 migrations apply lens-d1 --local",
    "migrate:local": "yes | wrangler d1 migrations apply lens-d1 --local",
    "migrate:remote": "yes | wrangler d1 migrations apply lens-d1 --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lens/shared": "workspace:*",
    "hono": "^4.13.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "~6.0.3",
    "wrangler": "^4.115.0"
  }
}
